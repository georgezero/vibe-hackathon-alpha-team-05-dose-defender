{
  "name": "dose-defender",
  "version": "0.1.0",
  "private": true,
  "description": "Tower defense where radiation particles advance on a patient. Place shielding to",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}